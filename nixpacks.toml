# Configuración de Nixpacks para Dokploy con PHP 8.4 y PostgreSQL
[phases.setup]
nixPkgs = ["...", "php84", "php84Extensions.pdo_pgsql", "php84Extensions.pgsql"]

[phases.build]
cmds = ["...", "npm run build"]
